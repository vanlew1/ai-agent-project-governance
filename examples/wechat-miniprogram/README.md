# WeChat Mini Program small example

Expected primary adapter: `wechat_miniprogram` from `project.config.json`; Node.js is auxiliary because the minimal test uses Node.

```powershell
npm test
```

One helper test passes. `project.private.config.json` is a sensitive path.
