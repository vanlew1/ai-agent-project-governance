# Upper workflow plan (synthetic)

Objective: update `src/compliant_change.py` and prove its focused test passes.

- Allowed changes: `src/`
- Denied changes: `restricted-change.txt`
- Required test: `python -m unittest discover -s tests`
- Completion condition: evidence is fresh when Closure runs.

This plan is intentionally small. It may be produced by Superpowers, `AGENTS.md`, or a team-specific workflow; the governance runtime does not trust its source automatically.
