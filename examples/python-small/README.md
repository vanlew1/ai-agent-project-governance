# Python small example

Expected adapter: `python` from `pyproject.toml`.

```powershell
python -m unittest discover -s tests
```

One test passes. A contract allowing only `src/**` blocks `secrets/**`.
