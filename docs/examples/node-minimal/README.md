# Node minimal synthetic demo

This static fixture demonstrates recognizable Node project markers. It is not a real adoption case and is not executed by this repository's test suite.

```bash
python scripts/agent_audit.py --project-root docs/examples/node-minimal
```

Use the recommended preset in a newly created project, then record scope, test, Git, and external-access boundaries before coding-agent work begins.
